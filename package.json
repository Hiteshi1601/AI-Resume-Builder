{
  "name": "ai-resume-builder-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Backend && npm install",
    "start": "cd Backend && npm start",
    "dev": "cd Backend && npm run dev"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
